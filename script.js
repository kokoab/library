const myLibrary = [];

function Book(){
    this.title = '';
    this.author = '';
    this.pages = 0;
    this.read = false;
}

function addBooktoLibrary(title, author, pages, read) {
    const book = {
        title: title,
    }
}